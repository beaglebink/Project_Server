#pragma once

// Editor-only widget removed from C++ source to avoid Packaging/UHT issues.
// Use `UFloorAssignerEditorLibrary` (BlueprintFunctionLibrary) for editor utilities.
// If you need an Editor Utility Widget, create it as a Blueprint only (Editor).